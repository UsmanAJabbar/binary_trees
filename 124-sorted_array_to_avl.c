#include "binary_trees.h"

/**
 * sorted_array_to_avl - converts an AVL tree to a sorted array
 * @array: pointer to heap
 * @size: heap size
 * Return: pointer to sorted array
 **/
avl_t *sorted_array_to_avl(int *array, size_t size)
{
}
