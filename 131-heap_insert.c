#include "binary_trees.h"

/**
 * heap_insert - insert a value in a heap
 * @root: pointer to tree
 * @value: value to insert into tree
 * Return: pointer to created node
 **/
heap_t *heap_insert(heap_t **root, int value)
{
}
